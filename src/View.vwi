
{
    // store.set("map overlay visible", "false")
    store.set("map overlay visible", "true")
    document.documentElement.style.setProperty('--hue', 200)
    document.documentElement.style.setProperty('--sat', "30%")
}

ImportJS: ChatGPT

Import: Foundation
Import: API/Map
Import: API/APIs
Import: API/TimeManager
// Import: Music/VoiceCapture
PageTitle: Infotainment
PageIcon: favicon.png


Wrapper
    display: {store.get("map overlay visible") == "true" ? "block" : "none"}
    Block
        position: fixed
        top: 0
        left: 0
        height: 100vh
        width: 100vw
        z-index: 0
        Block
            @id: map
            height: 100%
            width: 100%

    Import: MapOverlay

Wrapper
    display: {store.get("map overlay visible") == "false" ? "block" : "none"}
    Import: Entrance